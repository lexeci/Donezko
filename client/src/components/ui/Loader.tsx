import { Loading03Icon } from "hugeicons-react";

const Loader = () => {
	return (
		<div className="flex justify-center items-center">
			<Loading03Icon className="animate-spin h-5 w-5 text-white" />
		</div>
	);
};

export default Loader;
